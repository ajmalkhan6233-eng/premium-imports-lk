# HANDOFF — Read This First, Every New Session

This file is the bridge between the planning work done in Claude chat and the build work now happening entirely in Claude Code. Read it fully before doing anything. This is now the primary continuity document — treat it the way CLAUDE.md/SESSION_LOG.md are already treated in this project's discipline.

## WHO YOU'RE WORKING WITH
Ajmal — entrepreneur/developer, Sri Lanka, runs Premium Imports LK (Dubai→SL import retail) and a separate business, BATHCO. Wife Nushra handles customer WhatsApp sales. Ajmal works largely by voice-to-text on mobile, so instructions may arrive with transcription noise — interpret intent, don't get stuck on literal wording. He does NOT want repeated clarifying questions: make a reasonable call, state the assumption plainly, and proceed. He wants honest, direct, unsugarcoated feedback — including telling him when something is a bad idea or riskier than it sounds.

**How to actually talk to him:** be conversational, not just a command executor. Discuss trade-offs, flag concerns, ask the occasional real question when a decision genuinely needs his input (auth design, money-touching changes) — the same way this chat has been operating. Don't go silent and just output diffs; narrate what you're doing and why, the way a competent engineer would talk to the founder they're building for.

## HARD RULES (non-negotiable, every session)
- **BATHCO COMMAND is completely off-limits** during any Premium Imports LK work. Never open, query, or reference it. This is not a preference, it's a wall.
- AZMI in BATHCO is a personal payable account, never a vendor — irrelevant here but never forget it if BATHCO ever comes up.
- Bypass-permissions OFF before any database/auth work.
- No live server restart/deploy without Ajmal's explicit confirmation, when the change is significant (auth, money, data).
- Keep changes SMALL and INCREMENTAL by default (see Usage note below) — don't do sweeping multi-file rewrites unless explicitly asked for a big push.

## WHAT'S ALREADY DONE (verify against SESSION_LOG.md, don't just trust this summary blindly)
1. **Full 5-council security/quality audit** completed — AUDIT_REPORT.md exists at project root. Found strong points (atomic writes, daily backups, correct ledger math, hard-gated WhatsApp escalation, unspoofable wake phrase) and 5 ranked findings.
2. **Findings #4 (concurrency-safe numbering/writes) and #5 (PM2 process supervision)** — implemented and verified. Oversell race tested (3 requests, 1 stock, exactly 1 succeeds, other 2 get clean 409s). PM2 auto-restart confirmed (~3s recovery).
3. **Findings #2 (bank-account PIN-gate) and #3 (server-side price re-validation)** — CONFIRMED DONE, independently re-verified in a Claude Code session (2026-08-11, same session that completed Finding #1 below) by reading `server.js` directly: `bankDetailsChanged()`/`pinMatchesAnyUser()` still gate bank-detail writes, and `POST /api/orders` still re-derives price server-side from `db.products`. Both are also now wrapped by the Finding #1 session-auth layer (see #6).
4. **Storefront redesign (Phase 1)** — done and verified: monospace utility type for prices/tags, refined scroll-reveal easing, orchestrated hero load-in, visible keyboard focus rings, full prefers-reduced-motion support. All existing functionality (cart drawer, category tabs, WhatsApp button, live stock sync) confirmed intact.
   - **Stock:0 issue — RESOLVED (as "not a bug"), 2026-08-11:** confirmed via direct `data.json`/API inspection across multiple sessions that this is genuine real-world stock state (the actual products are at or near zero stock), not a sync or data-pipeline problem. This is an inventory/restocking question for the business, not an engineering issue — no code fix needed. Restock via GRN to resolve for real.
5. **App Modernization Phase 2 (internal POS/admin UI)** — fully implemented and verified: bill action cluster (Print/Download PDF/WhatsApp/Void), GRN photo/PDF upload, vendor CSV/PDF export, dashboard summary cards, new Bills history screen, search/filter across Sell/GRN/Vendors/Bills, designed empty states, low-stock badges. Two bugs found and fixed during verification: an unclickable restock button, and voided-bill numbers leaking into Dashboard/Reports totals. Logged in SESSION_LOG.md under "App Modernization Phase 2."
6. **Finding #1 — full API authentication — DONE, 2026-08-11.** `AUTH_COMMAND.md` was run in full: server-side session login (mirrors the existing `settings.users` PIN scheme, no new credential system), default-deny on `/api/data/:key` with a narrow field-filtered public allowlist (`settings`→shopName/whatsappNumber/bankDetails/categories only, `products`→no costPrice), role enforcement (settings/products writes admin-only), WhatsApp bridge given a scoped service credential, GRN-scan endpoints gated (spend protection). One real bug was found and fixed during verification: the initial `boot()` rewrite would have broken the login screen entirely for every user (fetched protected keys before showing the picker, 401 killed the whole flow) — caught by actually loading the app in a browser, not just a code read-through. Full inventory, design-decision reasoning (two places the command's own rules would have broken the storefront if followed too literally — resolved and documented), and a verification table are in `SESSION_LOG.md` under "Full API Authentication (Finding #1)". The original audit exploit (anonymous read of staff PINs / write of bank details) was directly re-attempted and confirmed now failing (401).
7. **Skills**: `CLAUDE_MD_ADDENDUM.md` (permanent skill-check-first rule) — confirmed appended to `CLAUDE.md`. Bulk skill install — **only the official `anthropics/skills` marketplace was installed** (`frontend-design`, `canvas-design`, `algorithmic-art`, `docx`/`pptx`/`xlsx`/`pdf`, plus a few more Anthropic bundles them with). The ~102-skill third-party bundle (`alirezarezvani/claude-skills`, a personal non-Anthropic GitHub repo) was **explicitly declined by Ajmal** when asked directly — flagged as an unverified third-party marketplace whose content (natural-language instructions loaded into every future session) can't be vetted. Not installed, and shouldn't be added without asking again first.
8. **Storefront redesign Phase 2 (Motion & Graphics) + AI Business Intelligence — 2026-08-11.** Both done and verified in a live browser; full detail in `SESSION_LOG.md`. Phase 2: product card micro-interactions, animated category-switch reflow, a restrained hero/skyline light-sweep, skeleton loaders, redesigned empty state (stock:0 is still genuine low inventory, not a bug — see item 4). AI layer: **Expenses** (new admin tab, soft-delete/admin-only-void), **Net Profit** dashboard card (revenue − COGS − expenses, auto-refreshing), and an **Ask AI** dashboard panel backed by a swappable Claude/local model backend and read-only data tools — wired end-to-end but **needs an Anthropic API key in `secrets.json` (`anthropicApiKey`) before it can actually answer anything**, same prerequisite GRN-scan already had.

## USAGE DISCIPLINE (important, standing instruction)
Ajmal is doing small, incremental edits right now, not large builds. Default to minimal, targeted changes. Don't burn a large session on a small ask. If a request is genuinely small, treat it as small — don't expand scope, add unrequested improvements, or refactor adjacent code unless asked. Flag it clearly if a "small" request turns out to actually require a larger change, and let him decide whether to proceed.

## NEXT STEPS
See NEXT_STEPS_ROADMAP.md (separate file) for the prioritized list — Priority 1 items #1–#3 are now all resolved as of 2026-08-11 (see "WHAT'S ALREADY DONE" above); Priority 2 is next up.
